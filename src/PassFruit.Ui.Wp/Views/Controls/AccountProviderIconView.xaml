<UserControl x:Class="PassFruit.Ui.Wp.Views.Controls.AccountProviderIconView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:Controls="clr-namespace:PassFruit.Ui.Wp.Views.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             cal:Bind.AtDesignTime="True"
             d:DataContext="{d:DesignInstance Controls:AccountProviderIconViewModel,
                                              IsDesignTimeCreatable=false}"
             d:DesignHeight="165"
             d:DesignWidth="203"
             FontFamily="{StaticResource PhoneFontFamilyNormal}"
             FontSize="{StaticResource PhoneFontSizeNormal}"
             Foreground="{StaticResource PhoneForegroundBrush}"
             mc:Ignorable="d">

    <Grid x:Name="LayoutRoot">
        <Rectangle>
            <Rectangle.Fill>
                <ImageBrush AlignmentX="Center"
                            AlignmentY="Center"
                            ImageSource="{Binding ImageSource}"
                            Stretch="None" />
            </Rectangle.Fill>
        </Rectangle>
    </Grid>
</UserControl>
